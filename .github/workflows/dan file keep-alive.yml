name: Keep Supabase Alive

on:
  schedule:
    - cron: '0 */6 * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: "${{ secrets.SUPABASE_URL }}/rest/v1/renco_db_snapshot?select=id&limit=1" \
        run: |
          curl -s \
            "${{ secrets.SUPABASE_URL }}/rest/v1/ping_table?select=id&limit=1" \
            -H "apikey: ${{ se
